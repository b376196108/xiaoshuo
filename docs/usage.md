# 使用指南

1. 克隆仓库并在已激活的 `.venv` 中运行 `pip install -r requirements.txt` 安装依赖。
2. 复制 `.env.example` 为 `.env`，填入 B 机的连接信息与密码。
3. 运行 `powershell -ExecutionPolicy Bypass -File .\scripts\scaffold.ps1`，确保项目结构齐全后再展开开发。

其它使用步骤（比如具体的 CLI 命令）将在编排器与 Agents 实现后补充。

