# Rolling Recap（滚动摘要）

## 规则（强制）
- 字数控制：必须保持在 1500–3000 字范围内；超过上限时必须压缩旧信息，只保留“主线事实 + 情绪走向 + 关键冲突 + 未解伏笔概览”。
- 更新方式：每次新章完成后更新一次；不得引入正文未出现的新事实。
- 事实优先：与 `state/current_state.json` 冲突时，以 `state/` 为准并在 QA 报告中标注差异。

## 当前摘要（初始为空）
- TBD

